spec:
  volumeClaimTemplates:
    - metadata:
        name: jenkins
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 8Gi
        storageClassName: standard  # Ensure this is set correctly
